const tasks  = require('./data/tasks');
//const { addTask, removeTask, updateTaskStatus, listTasks, sortTasksByStatus } = require('./tasks');

// Sample usage

// uncomment this code after immplementing the functions:

// addTask(tasks, { id: 4, title: 'Task 4', status: 'pending', subTasks: [] });
// updateTaskStatus(tasks, 4, 'in progress');
// removeTask(tasks, 2);
// sortTasksByStatus(tasks);
// listTasks(tasks);